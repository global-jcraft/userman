version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: userman-postgres
    restart: always
    environment:
      POSTGRES_DB: userman
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: dev_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: